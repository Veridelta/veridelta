# Veridelta

**Semantic diffing for mission-critical data pipelines.**

Veridelta is a high-performance Python engine.

## Installation

Install the stable version from PyPI:

```bash
pip install veridelta
```

Or, if you are using uv:

```bash
uv add veridelta
```